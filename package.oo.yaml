version: 0.0.2
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: poetry add pandas
  bootstrap: |
    npm install
    poetry install --no-root
dependencies:
  oomol-preview: 0.0.5
icon: ./icon.png
name: svg2png
description: "%svg-png%"
displayName: "%svg-to-png%"
repository: https://github.com/oomol-blocks/svg2png
releaseNotes: update
